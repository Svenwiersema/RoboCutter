[project]
name = "robocutter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "pillow"]
ui = ["PySide6", "qrcode", "python-barcode"]
build = ["pyinstaller"]

[tool.pytest.ini_options]
pythonpath = ["src"]
